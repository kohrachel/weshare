/**
 Contributors
 Rachel Huiqi: 1 hour
 */

// Mock for "@/firebaseConfig" used by your components during tests.
// Prevents importing the real Firebase ESM SDK.
export const db = {
  // Optional: add methods if any code calls db directly (yours uses doc/getDoc, which we mock via firebase/firestore)
};
